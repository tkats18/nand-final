class Main {

    function void main() {
        var GameManager game;
        var Random random;

        let random = Random.new();
        do random.getSeed();

        let game = GameManager.new(random);
        do game.fillBoard();
        do game.startGame();
        
        return;
    }
}